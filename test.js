

    var numbersList = [3, 5, 1, 8, 2];
    var sortedList = [];
    
    function pusher() {
        sortedList.push(numbersList);
        console.log(sortedList);
    };

    module.exports = pusher;

    // var sorter = function (a, b){ //causes an array to be sorted numerically and ascending
    //     return (a - b);
    // };

    // var sortedList = function () {
    //     sortedList.sort();
    // };

